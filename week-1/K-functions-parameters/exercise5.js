// Declare your function here
function createLongGreeting(nombre, edad) {
  return (greeting =
    "Hello, my name is " + nombre + " and I`m " + edad + " years old");
}

greeting = createLongGreeting("Ricardo", 35);

console.log(greeting);
